apiVersion: v1
kind: Service
metadata:
  name: dk-k8s-redhat-linux-service
spec:
  type: NodePort
  selector:
    app: dk-k8s-redhat-linux
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080