apiVersion: apps/v1
kind: Deployment
metadata:
  name: dk-k8s-redhat-linux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dk-k8s-redhat-linux
  template:
    metadata:
      labels:
        app: dk-k8s-redhat-linux
    spec:
      securityContext:
        fsGroup: 2000   # 👈 THIS FIXES PERMISSION
      containers:
      - name: dk-k8s-redhat-linux
        image: avadhut18/dk-k8s-redhat-linux:v1 
        ports:
        - containerPort: 80
        securityContext:
          runAsUser: 1000
        volumeMounts:
        - name: storage-volume
          mountPath: /app/data
      volumes:
      - name: storage-volume
        persistentVolumeClaim:
          claimName: file-pvc
      